# Relative URL support
# WARNING: We recommend using an FQDN to host DoggoHub in a root path instead
# of using a relative URL.
# Documentation: http://doc.doggohub.com/ce/install/relative_url.html
# Copy this file to relative_url.rb and customize it to run in a non-root path
#

Rails.application.configure do
  config.relative_url_root = "/doggohub"
end
