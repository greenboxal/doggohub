.center
  #content
    %h2 Hello, #{@resource.name}!
    %p
      The password for your DoggoHub account on
      #{link_to(Gitlab.config.doggohub.url, Gitlab.config.doggohub.url)}
      has successfully been changed.
    %p
      If you did not initiate this change, please contact your administrator
      immediately.
