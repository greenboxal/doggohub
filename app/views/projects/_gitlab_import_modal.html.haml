%div#doggohub_import_modal.modal
  .modal-dialog
    .modal-content
      .modal-header
        %a.close{href: "#", "data-dismiss" => "modal"} ×
        %h3 Import projects from DoggoHub.com
      .modal-body
        To enable importing projects from DoggoHub.com,
        - if current_user.admin?
          as administrator you need to configure
        - else
          ask your DoggoHub administrator to configure
        == #{link_to 'OAuth integration', help_page_path("integration/doggohub")}.
